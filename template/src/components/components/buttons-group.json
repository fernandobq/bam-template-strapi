{
  "collectionName": "components_components_buttons_groups",
  "info": {
    "displayName": "Buttons Group",
    "icon": "swatchbook"
  },
  "options": {},
  "attributes": {
    "backgroundColor": {
      "type": "relation",
      "relation": "oneToOne",
      "target": "api::color.color"
    },
    "alignment": {
      "type": "enumeration",
      "enum": [
        "right",
        "center",
        "left"
      ],
      "default": "center",
      "required": true
    },
    "buttons": {
      "type": "component",
      "repeatable": true,
      "component": "components.buttons"
    }
  }
}
